interface:
  display_name: "Hermes Obsidian Controlled Query"
  short_description: "Run governed vault queries with auditable retrieval traces."
  default_prompt: "Use this skill for a governed query against the intranet vault configured in config/intranet.json, currently /opt/data/phq/testVault. Provide the question; the skill keeps governed knowledge read-only but must create the default non-authoritative Obsidian query trace unless the user explicitly opts out, while handling search order, evidence packets, QA labels, and hierarchical navigation."
